{
  "name": "@remly/channel",
  "description": "A channel resolver and factory library",
  "version": "0.2.1",
  "keywords": [],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=14"
  },
  "author": "Yuan Tao",
  "license": "MIT",
  "scripts": {
    "acceptance": "lb-mocha \"dist/__tests__/acceptance/**/*.js\"",
    "build": "lb-tsc",
    "clean": "lb-clean remly-channel*.tgz dist *.tsbuildinfo package",
    "pretest": "npm run clean && npm run build",
    "test": "lb-mocha --allow-console-logs  \"dist/__tests__/**/*.js\"",
    "unit": "lb-mocha --allow-console-logs  \"dist/__tests__/unit/**/*.js\"",
    "verify": "npm pack && tar xf remly-channel*.tgz && tree package && npm run clean"
  },
  "dependencies": {
    "@libit/error": "^0.3.6",
    "@remly/server": "^0.9.2",
    "debug": "^4.3.2",
    "ts-essentials": "^7.0.3"
  },
  "devDependencies": {
    "@loopback/build": "^7.0.0",
    "@loopback/eslint-config": "^11.0.0",
    "@loopback/testlab": "^3.4.2",
    "@remly/tcp": "^0.5.17",
    "@remly/ws": "^0.5.17",
    "@types/debug": "^4.1.7",
    "@types/node": "^16.4.1",
    "remly-channel-throwing": "file:./dist/__tests__/fixtures/remly-channel-throwing"
  },
  "files": [
    "README.md",
    "dist",
    "src",
    "!*/__tests__"
  ],
  "publishConfig": {
    "access": "public"
  }
}
