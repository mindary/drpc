{
  "name": "@remly/example-ecdsa-id",
  "description": "A authentication example with ECDSA",
  "version": "0.2.14",
  "keywords": [],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=14"
  },
  "author": "Yuan Tao",
  "license": "MIT",
  "private": true,
  "scripts": {
    "acceptance": "lb-mocha \"dist/__tests__/acceptance/**/*.js\"",
    "build": "lb-tsc",
    "clean": "lb-clean remly-example-ecdsa*.tgz dist *.tsbuildinfo package",
    "pretest": "npm run clean && npm run build",
    "test": "lb-mocha --allow-console-logs  \"dist/__tests__/**/*.js\"",
    "unit": "lb-mocha --allow-console-logs  \"dist/__tests__/unit/**/*.js\"",
    "verify": "npm pack && tar xf remly-example-ecdsa*.tgz && tree package && npm run clean",
    "rebuild": "npm run clean && npm run build",
    "prestart": "npm run rebuild",
    "start": "node ."
  },
  "dependencies": {
    "@libit/josa": "^0.4.4",
    "@remly/core": "^0.7.1",
    "@remly/server": "^0.7.1",
    "@remly/tcp": "^0.5.11"
  },
  "devDependencies": {
    "@libit/josa": "^0.4.4",
    "@loopback/build": "^7.0.0",
    "@loopback/eslint-config": "^11.0.0",
    "@loopback/testlab": "^3.4.2",
    "@remly/tcp-client": "^0.3.8",
    "@types/node": "^16.4.13",
    "get-port": "^5.1.1"
  },
  "files": [
    "README.md",
    "dist",
    "src",
    "!*/__tests__"
  ]
}
